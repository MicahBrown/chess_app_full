<!DOCTYPE html>
<html>
  <head>
    <title>ChessApp</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div>
      <p>
        <% if signed_in? %>
          <%= link_to "Log Out", destroy_user_session_path, method: :delete %>
        <% else %>
          <%= link_to "Log In", new_user_session_path %>
        <% end %>
      </p>

      <% if signed_in? %>
        <div class="pure-menu pure-menu-horizontal" style="text-align: center;">
          <ul class="pure-menu-list">
            <li class="pure-menu-item"><%= link_to "Profile", user_path(current_user), class: "pure-menu-link" %></li>
            <li class="pure-menu-item"><%= link_to "Play", games_path, class: "pure-menu-link" %></li>
            <li class="pure-menu-item"><%= link_to "Clubs", clubs_path, class: "pure-menu-link" %></li>
          </ul>
        </div>
      <% end %>

      <% if flash.any? %>
        <% flash.each do |type, msg| %>
          <div><%= msg %></div>
        <% end %>
      <% end %>
    </div>

    <%= yield %>
  </body>
</html>
