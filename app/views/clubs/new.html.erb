<h1>New Club</h1>
<%= form_for @club, html: {class: "pure-form pure-form-stacked"} do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit class: "pure-button pure-button-primary" %>
<% end %>